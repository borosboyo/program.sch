import React from 'react';
import AppNavbar from './AppNavbar';
import {Container} from "reactstrap";

const Home = () => {
    return (
        <div>
            <AppNavbar/>
            <Container fluid>
            </Container>
        </div>
    );
}

export default Home;
